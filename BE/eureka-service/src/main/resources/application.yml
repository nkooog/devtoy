spring:
    application:
        name: eureka-service
    profiles:
        active: local
server:
    port: 45001


eureka:
    instance:
        prefer-ip-address: false
    client:
        register-with-eureka: false
        fetch-registry: false
    server:
        enable-self-preservation: true
