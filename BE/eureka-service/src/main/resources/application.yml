server:
  port: 45001

spring:
  application:
    name: eureka-service
  profiles:
    active: local

eureka:
  instance:
    prefer-ip-address: false

  client:
    register-with-eureka: false
    fetch-registry: false

  server:
    enable-self-preservation: true